# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RestaurantManager2' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RestaurantManager2

  pod 'Firebase/Core'
  pod 'FirebaseUI/Auth'

  target 'RestaurantManager2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RestaurantManager2UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
